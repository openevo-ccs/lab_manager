<!--
CONTRIBUTING.md skeleton — OpenEvo CCS Lab house style, v1 (2026-07-22).

This is a SHAPE, not boilerplate to paste verbatim. Extracted from the three
CONTRIBUTING.md files that already existed in this ecosystem before this
skeleton did (conceptbase, curriculum-agents, curriculum-evolution) — each of
those is genuinely different in its "Proposing a change" content because each
repo's actual governance is different. Copying prose across repos produces a
document that's technically present but describes a process that doesn't
exist for that repo — worse than no CONTRIBUTING.md, because it's actively
misleading. Fill in each section from what's actually true of the target
repo; delete this comment block in the rolled-out file.

To propose a change to THIS skeleton (the shape, not any one repo's filled-in
copy): open a PR against lab_manager modifying this file, same review
discipline as everything else in this ecosystem. See
docs/design-notes/ecosystem-cleanliness-and-maintenance-plan.md §6 item 3.

Sections below are a menu, not a mandate — omit ones that don't apply
(e.g. a repo with no versioning/lifecycle model skips that section).
-->

# Contributing to {REPO_NAME}

**Status:** {one line — is there a heavier authoritative doc this defers to
(a GOVERNANCE.md, an upstream project's own process, a sibling repo's RFC
pipeline)? Link it. If this file *is* the authoritative process, say so.}

## Before you start

{What should a contributor read first? What does this repo NOT accept
(scope boundaries), if that's a real constraint worth stating up front?}

## Proposing a change

{The actual process — as many or as few steps as genuinely exist today.
Don't invent formality (a review-board, an RFC template) that doesn't exist
yet just to look complete. A one-line "open a PR, get one approval, merge"
is an honest, complete answer for a repo with no heavier process.}

## Where things live

{A short map of the repo's real structure, only if it's not already
obvious from the README — skip this section if it'd just duplicate the
README's own structure section.}

## Questions

Open a GitHub issue, or reach out to the OpenEvo Computational Curriculum
Studies Lab ([openevo.eva.mpg.de](http://openevo.eva.mpg.de)).
